import { render } from 'enzyme';
import React from 'react';

import Recipe from './Recipe';

describe("Single recipe page testing", () => {
  // let wrapper;
  // beforeEach(() => {
  //   wrapper = render(<Recipe />);
  // });
  
  test("render title", () => {
    
  });

});